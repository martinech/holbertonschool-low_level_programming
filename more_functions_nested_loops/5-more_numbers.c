#include <stdio.h>
#include <string.h>
#include "main.h"

void
more_numbers(void)
{
	int i,x;
	
	for (x = 0 ; i <= 10; x++)
		for (i = '0'; i <= '14'; i++)
		{
			_putchar(i);
		}
		_putchar('\n');
}
