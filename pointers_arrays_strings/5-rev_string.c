#include <stdio.h>
#include "main.h"

void
rev_string(char *s)
{

int c[1000];
int n;
int a;

for (n = 0; s[n]; n++)
	{
		continue;
	}
n -= 1;

for (; s[n]; n--)
{
	c[a] = s[n];
	a++;
}
for (n = 0; s[n]; n++)
{
	s[n] = c[n];
}
}
