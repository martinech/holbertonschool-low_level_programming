#include <stdio.h>
#include "main.h"

void
rev_string(char *s)
{

int len = 0;
int c[1000];
int n;

for (n = 0; s[n] != '\0'; n++)
{
	continue
}

for (len; len > 0; len--)
{
	c[len]=s[len];
}
return(c);
}
