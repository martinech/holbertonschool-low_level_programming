#include <stdio.h>
#include "main.h"

void
rev_string(char *s)
{

int c[1000];
int n;
int a;

for (n = 0; s[n] != '\0'; n++)
{
	continue;
}

for (n; n > 0; n--)
{
	c[n]=s[n];
}

for (a; a <0; a--)
return(c);
}
