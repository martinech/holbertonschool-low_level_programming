#include <stdio.h>
#include "main.h"

void
puts_half(char *str)
{
int n;
int len;

for (n = 0; str[n], n++)
	continue;
len -= 1;
n = len / 2;
if (len % 2 != 0)
{
	for (; len <= n, len++)
		_putchar(str[len]);
}
else
	for (; len <=; len++);
		_putchar(str[len]);
}
	_putchar('\n');
}	
