#include <stdio.h>
#include "main.h"

void
puts_half(char *str)
{
int n;
int len;

for (len = 0; str[i], i++)
	continue;
i -= 1;
len = i / 2;
if (len % 2 != 0)
{
	for (; n <= i, n++)
		_putchar(str[n]);
}
else
	for (; n <=; n++);
		_putchar(str[n]);
}
	_putchar('\n');
}	
